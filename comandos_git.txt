comando git
git init "dentro de la carpeta"
git status
git add "file"
git config --global user.email "email.com"
git config --global user.name "minombre"
git commit -m "comentario"
git checkout "comandos_git.txt"


crear gitignore
git add .gitignore

git branch nueva_version  ----crea la rama
git checkout nueva_version --- entro en la rama 
 hago cambios dentro de la verison
 
 git add .
 git commit
 
 git checkout master

comprobamos it

 
 
git remote add origin https://github.com/leosistemas/socios.git
git push -u origin master

git clone https://github.com/leosistemas/socios.git
eh?sd